A plain‑text App Designer app consists of:
- MATLAB class code (.m)
- UI layout expressed as XML

Layout XML may appear:
- As a standalone XML document, or
- Embedded in a commented appendix using %[app:layout]

The translator may emit either form.
Golden exemplars use standalone XML for diffability.